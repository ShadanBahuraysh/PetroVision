// Stub - never executed at runtime, only satisfies the compiler
// when neither dart:html nor dart:io resolves (shouldn't happen).
Future<void> saveFileToDevice(List<int> bytes, String fileName) async {
  throw UnsupportedError('Download not supported on this platform.');
}
